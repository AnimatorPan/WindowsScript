# 文档管家 - 依赖列表
# 安装命令: pip install -r requirements.txt

# GUI框架
PyQt6>=6.4.0

# 文件监控
watchdog>=3.0.0

# 数据库（Python标准库sqlite3，无需安装）

# 文件类型检测
python-magic>=0.4.27; sys_platform != 'win32'
python-magic-bin>=0.4.14; sys_platform == 'win32'

# 文档预览支持
PyMuPDF>=1.23.0  # PDF预览
pillow>=10.0.0   # 图片处理

# 工具库
send2trash>=1.8.0  # 安全删除到回收站

# 打包工具（开发用）
pyinstaller>=6.0.0
